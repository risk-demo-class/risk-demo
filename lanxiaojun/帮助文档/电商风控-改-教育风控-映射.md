# 电商风控 → 教育风控：全层适配映射

> 目标：复用电商版的 7 步 Pipeline 架构（event→feature→rule→ML→score→decision），
> 将业务层从电商替换为教育行业。

---

## 一、改动总览（按层）

| 层 | 文件 | 改动量 | 说明 |
|----|------|--------|------|
| 业务 ORM | `app/models_business.py` | **重写** | 10 张教育表替换 17 张电商表 |
| 风控 ORM | `app/models_risk.py` | **改枚举** | event_type/rule_category 枚举值 |
| Pydantic | `app/schemas.py` | **改 Literal** | event_type + 新字段 |
| Feature | `app/engine/feature.py` | **重写** | ~20 维教育特征替换 25 维电商特征 |
| Event | `app/service/event.py` | **改** | 补全逻辑 + 黑名单查法 |
| Validator | `app/service/validator.py` | **重写** | 学生/课程/退费等新校验 |
| Router | `app/routers/risk.py` | **微调** | 新 request schemas |
| Router | `app/routers/pages.py` | **微调** | 页面路由 |
| Config | `app/config.py` | **微调** | 新阈值（可选） |
| 黑名单 | `app/service/case.py` | **微调** | 4 类黑名单 |
| 规则库 | `sql/init_risk_tables.sql` | **重写** | 25 条教育规则 |

---

## 二、业务 ORM 对照（`app/models_business.py`）

### 电商（原）→ 教育（新）

```python
# ========== 原电商 17 张表 ==========
class UserInfo(Base):             # 用户（只有user_id）
class Region(Base):               # 地区
class ProductCategory(Base):      # 商品分类
class SkuInfo(Base):              # 商品
class OrderInfo(Base):            # 订单
class OrderDetail(Base):          # 订单明细
class OrderStatus(Base):          # 订单状态
class ReceiveInfo(Base):          # 收货地址
class Logistics(Base):            # 物流
class OrderLogistics(Base):       # 订单物流
class LogisticsCompany(Base):     # 物流公司
class LogisticsComplaint(Base):   # 物流投诉
class LogisticsComplaintsRecord(Base):  # 物流投诉记录
class Postsale(Base):             # 售后
class PostsaleLogistics(Base):    # 售后物流
class PostsaleReason(Base):       # 售后原因
class PostsaleStatus(Base):       # 售后状态

# ========== 新教育 10 张表 ==========
class UserInfo(Base):
    """用户表 — 扩充: 加 role(学生/老师/家长) 和 real_name_status"""
    __tablename__ = "user_info"
    user_id: Mapped[str] = mapped_column(String(50), primary_key=True)
    name: Mapped[str] = mapped_column(String(100), comment="姓名")
    role: Mapped[str] = mapped_column(Enum("学生","老师","家长"), comment="角色")
    phone: Mapped[str] = mapped_column(String(20), comment="手机号")
    real_name_status: Mapped[int] = mapped_column(Integer, default=0, comment="实名认证 0=未认证 1=已认证")
    account_age_days: Mapped[int] = mapped_column(Integer, default=0, comment="账号年龄(天)")
    register_at: Mapped[datetime] = mapped_column(DateTime, comment="注册时间")

class StudentProfile(Base):
    """学籍档案 — 全新"""
    __tablename__ = "student_profile"
    profile_id: Mapped[str] = mapped_column(String(50), primary_key=True)
    user_id: Mapped[str] = mapped_column(String(50), comment="关联用户ID")
    student_id: Mapped[str] = mapped_column(String(50), comment="学号")
    school_name: Mapped[str] = mapped_column(String(200), comment="学校名称")
    grade: Mapped[str] = mapped_column(String(50), comment="年级/班级")
    id_card_hash: Mapped[str] = mapped_column(String(128), comment="身份证号(SHA-256)")
    parent_phone: Mapped[str] = mapped_column(String(20), comment="家长手机号")

class TeacherInfo(Base):
    """教师信息 — 全新"""
    __tablename__ = "teacher_info"
    teacher_id: Mapped[str] = mapped_column(String(50), primary_key=True)
    user_id: Mapped[str] = mapped_column(String(50), comment="关联用户ID")
    name: Mapped[str] = mapped_column(String(100), comment="教师姓名")
    cert_no: Mapped[str] = mapped_column(String(100), comment="资质证书编号")
    teach_years: Mapped[int] = mapped_column(Integer, default=0, comment="教龄(年)")
    avg_rating: Mapped[float] = mapped_column(Numeric(3,2), default=0.0, comment="平均评分")
    course_count: Mapped[int] = mapped_column(Integer, default=0, comment="开课数量")

class Course(Base):
    """课程 — 替代电商的 SkuInfo"""
    __tablename__ = "course"
    course_id: Mapped[str] = mapped_column(String(50), primary_key=True)
    name: Mapped[str] = mapped_column(String(200), comment="课程名称")
    category: Mapped[str] = mapped_column(String(50), comment="课程类别(考研/公考/职业/语言/素质)")
    price: Mapped[Decimal] = mapped_column(Numeric(10,2), comment="课程价格")
    total_hours: Mapped[int] = mapped_column(Integer, comment="总课时")
    teacher_id: Mapped[str] = mapped_column(String(50), comment="授课教师ID")
    publish_date: Mapped[datetime] = mapped_column(DateTime, comment="上架时间")
    status: Mapped[str] = mapped_column(Enum("上架","下架"), comment="课程状态")

class OrderInfo(Base):
    """订单 — 加教育专属字段"""
    __tablename__ = "order_info"
    order_id: Mapped[str] = mapped_column(String(50), primary_key=True)
    user_id: Mapped[str] = mapped_column(String(50), comment="用户ID")
    course_id: Mapped[str] = mapped_column(String(50), comment="课程ID")
    amount: Mapped[Decimal] = mapped_column(Numeric(10,2), comment="订单金额")
    pay_time: Mapped[datetime] = mapped_column(DateTime, comment="支付时间")
    study_goal: Mapped[str] = mapped_column(String(200), nullable=True, comment="学习目标")
    expected_finish_days: Mapped[int] = mapped_column(Integer, nullable=True, comment="预计完成天数")
    order_status: Mapped[str] = mapped_column(Enum("待支付","已支付","已退款","已关闭"), comment="订单状态")

class LearningProgress(Base):
    """学习进度 — 全新"""
    __tablename__ = "learning_progress"
    progress_id: Mapped[str] = mapped_column(String(50), primary_key=True)
    user_id: Mapped[str] = mapped_column(String(50), comment="用户ID")
    course_id: Mapped[str] = mapped_column(String(50), comment="课程ID")
    total_minutes: Mapped[int] = mapped_column(Integer, default=0, comment="累计学习时长(分钟)")
    completion_rate: Mapped[float] = mapped_column(Numeric(5,2), default=0.0, comment="完成率(%)")
    last_active_at: Mapped[datetime] = mapped_column(DateTime, comment="最后活跃时间")
    chapter_progress: Mapped[str] = mapped_column(Text, nullable=True, comment="章节进度(JSON)")

class RefundRequest(Base):
    """退费申请 — 替代电商 Postsale"""
    __tablename__ = "refund_request"
    refund_id: Mapped[str] = mapped_column(String(50), primary_key=True)
    order_id: Mapped[str] = mapped_column(String(50), comment="关联订单ID")
    user_id: Mapped[str] = mapped_column(String(50), comment="用户ID")
    reason: Mapped[str] = mapped_column(String(500), comment="退费原因")
    study_minutes_before_refund: Mapped[int] = mapped_column(Integer, default=0, comment="退费前已学时长(分钟)")
    refund_amount: Mapped[Decimal] = mapped_column(Numeric(10,2), comment="退费金额")
    status: Mapped[str] = mapped_column(Enum("待审核","已通过","已拒绝"), comment="退费状态")
    apply_time: Mapped[datetime] = mapped_column(DateTime, comment="申请时间")

class Complaint(Base):
    """投诉举报 — 全新"""
    __tablename__ = "complaint"
    complaint_id: Mapped[str] = mapped_column(String(50), primary_key=True)
    user_id: Mapped[str] = mapped_column(String(50), comment="投诉人")
    course_id: Mapped[str] = mapped_column(String(50), comment="被投诉课程")
    complaint_type: Mapped[str] = mapped_column(Enum("虚假宣传","侵权盗版","内容违规","师资不符"), comment="投诉类型")
    content: Mapped[str] = mapped_column(Text, comment="投诉内容")
    status: Mapped[str] = mapped_column(Enum("待处理","已处理","已驳回"), comment="处理状态")
    create_time: Mapped[datetime] = mapped_column(DateTime, comment="创建时间")

class PaymentAccount(Base):
    """支付账户 — 保留但强化监控"""
    __tablename__ = "payment_account"
    account_id: Mapped[str] = mapped_column(String(50), primary_key=True)
    user_id: Mapped[str] = mapped_column(String(50), comment="用户ID")
    payment_type: Mapped[str] = mapped_column(Enum("微信","支付宝","银行卡"), comment="支付方式")
    account_hash: Mapped[str] = mapped_column(String(128), comment="账户标识(SHA-256)")
    bind_time: Mapped[datetime] = mapped_column(DateTime, comment="绑定时间")
    is_verified: Mapped[int] = mapped_column(Integer, default=0, comment="是否验证")

class BlacklistExtra(Base):
    """黑名单扩展 — type 加 4 类"""
    __tablename__ = "blacklist_extra"
    entry_id: Mapped[str] = mapped_column(String(50), primary_key=True)
    type: Mapped[str] = mapped_column(
        Enum("学号","身份证","设备指纹","支付账号"), comment="黑名单类型")
    value: Mapped[str] = mapped_column(String(256), comment="黑名单值")
    reason: Mapped[str] = mapped_column(Text, comment="拉黑原因")
    expire_at: Mapped[datetime] = mapped_column(DateTime, comment="过期时间")

class DeviceFingerprint(Base):
    """设备指纹 — 新增，防团伙刷课"""
    __tablename__ = "device_fingerprint"
    device_id: Mapped[str] = mapped_column(String(128), primary_key=True)
    user_id: Mapped[str] = mapped_column(String(50), comment="最近关联用户")
    fingerprint_hash: Mapped[str] = mapped_column(String(256), comment="指纹SHA-256")
    first_seen: Mapped[datetime] = mapped_column(DateTime, comment="首次出现")
    last_seen: Mapped[datetime] = mapped_column(DateTime, comment="最后出现")
    os: Mapped[str] = mapped_column(String(50), comment="操作系统")
    browser: Mapped[str] = mapped_column(String(50), comment="浏览器")
    ip: Mapped[str] = mapped_column(String(50), comment="最近IP")
```

---

## 三、风控 ORM 枚举变更（`app/models_risk.py`）

### event_type 枚举

```python
# 电商版（原）
Enum("下单", "支付", "售后申请", "物流投诉", name="event_type_enum")

# 教育版（新）
Enum("register", "login", "real_name_auth", "purchase",
     "coupon_claim", "course_watch", "refund_apply", "complaint",
     name="event_type_enum")
```

### rule_category 枚举

```python
# 电商版（原）
Enum("订单欺诈", "支付风险", "账户风险", "售后滥用", "地址风险", "物流风险",
     name="rule_category_enum")

# 教育版（新）
Enum("注册风险", "账号风险", "营销作弊", "刷课行为", "退费欺诈",
     "支付风险", "内容安全", "师资风险",
     name="rule_category_enum")
```

### 其他表无需改结构（但表名和数据需要重建）

| 表 | 电商 | 教育 | 是否改结构 |
|----|------|------|-----------|
| `risk_rule` | 电商规则 30 条 | 教育规则 25 条 | 不改表，只改数据 |
| `risk_event` | event_type=下单/支付… | event_type=register/… | 改枚举 |
| `risk_feature` | 25 维特征 → risk_assessment | 20 维特征 → ... | 不改表 |
| `risk_assessment` | 不变 | 不变 | 不改表 |
| `risk_case` | 不变 | 不变 | 不改表 |
| `risk_blacklist` | type=用户/地址/手机号 | type=用户/学号/身份证/设备指纹/支付账号 | 改枚举 |
| `risk_user_profile` | 不变 | 不变 | 不改表 |
| `risk_action_log` | 不变 | 不变 | 不改表 |
| `risk_alert` | 不变 | 不变 | 不改表 |

---

## 四、Schema 变更（`app/schemas.py`）

```python
# ========== 教育版 RiskCheckRequest ==========
class RiskCheckRequest(BaseModel):
    event_type: Literal["register", "login", "real_name_auth", "purchase",
                        "coupon_claim", "course_watch", "refund_apply", "complaint"]
    source_id: str = Field(description="关联业务ID (order_id / refund_id 等)")
    user_id: str
    # 教育专属可选字段
    course_id: Optional[str] = None
    device_id: Optional[str] = None
    ip: Optional[str] = None
    event_data: Optional[dict] = None

# ========== 教育版 RuleCreate/Update ==========
class RuleCreate(BaseModel):
    rule_id: str
    rule_name: str
    rule_category: Literal["注册风险", "账号风险", "营销作弊", "刷课行为",
                           "退费欺诈", "支付风险", "内容安全", "师资风险"]
    event_type: Literal["register", "login", "real_name_auth", "purchase",
                        "coupon_claim", "course_watch", "refund_apply",
                        "complaint", "通用"] = "通用"
    # ... 其余字段与电商版一致

# ========== 决策相关的枚举（与电商版一致）==========
# risk_level: "低" | "中" | "高" | "极高"
# decision: "通过" | "标记" | "人工审核" | "拒绝"
# 这两个枚举值保持不动（四档决策是通用的）
```

---

## 五、特征工程变更（`app/engine/feature.py`）

### 电商 25 维 → 教育 20 维

```python
# ========== 用户维度 (~8个) ==========
async def _feat_user_account_age(db, user_id) -> float:      # 账号年龄(天)
async def _feat_user_real_name(db, user_id) -> float:        # 是否实名(0/1)
async def _feat_user_role(db, user_id) -> float:             # 角色(学生=1/老师=2/家长=3)
async def _feat_user_total_orders(db, user_id) -> float:     # 历史报名总数
async def _feat_user_orders_30d(db, user_id) -> float:       # 近30天报名数
async def _feat_user_avg_completion(db, user_id) -> float:   # 历史平均完成率
async def _feat_user_refund_rate(db, user_id) -> float:      # 历史退费率
async def _feat_user_complaint_count(db, user_id) -> float:  # 历史投诉次数

# ========== 课程维度 (~5个) ==========
async def _feat_course_price(db, course_id) -> float:          # 课程价格
async def _feat_course_category(db, course_id) -> float:       # 课程类别(编码)
async def _feat_course_total_hours(db, course_id) -> float:    # 总课时
async def _feat_course_teacher_rating(db, course_id) -> float: # 授课教师评分
async def _feat_course_teacher_tenure(db, course_id) -> float: # 老师入驻天数

# ========== 订单/支付维度 (~4个) ==========
async def _feat_order_amount(db, user_id, course_id) -> float:  # 本次订单金额
async def _feat_order_pay_time_hour(db, order_id) -> float:     # 支付时段(0-23)
async def _feat_order_multicourse(db, user_id) -> float:        # 同时报名课程数
async def _feat_payment_account_count(db, user_id) -> float:    # 绑定的支付账户数

# ========== 行为维度 (~3个) ==========
async def _feat_behavior_today_minutes(db, user_id) -> float:           # 今日学习时长
async def _feat_behavior_speed_ratio(db, user_id, course_id) -> float:  # 播放速度(倍数)
async def _feat_behavior_night_ratio(db, user_id) -> float:             # 凌晨学习占比(0-1)
```

---

## 六、Validator 变更（`app/service/validator.py`）

```python
# event_type → source_id 校验字典（新）
_EVENT_SOURCE_VALIDATORS = {
    ("purchase",):          (OrderInfo,   "order_id",    None,   "订单",   400),
    ("course_watch",):      (OrderInfo,   "order_id",    None,   "订单",   400),
    ("refund_apply",):      (RefundRequest, "refund_id",  None,   "退费申请", 400),
    ("complaint",):         (Complaint,   "complaint_id", None,   "投诉",   400),
    ("register", "login", "real_name_auth", "coupon_claim"):
                            (UserInfo,    "user_id",      None,   "用户",   404),
}

# 新增的归属校验
async def ensure_course_belongs_to_user(db, course_id, user_id) -> None:
    """防水平越权：检查课程是否属于该用户的已报名订单"""

async def validate_risk_check_request(db, request):
    # 1. 用户存在
    await ensure_user_exists(db, request.user_id)
    # 2. source_id 匹配校验
    await ensure_source_matches_event_type(db, request)
    # 3. purchase/refund_apply 时校验订单归属
    if request.event_type in ("purchase", "course_watch"):
        await ensure_course_belongs_to_user(db, request.course_id, request.user_id)
```

---

## 七、Event 变更（`app/service/event.py`）

```python
async def _enrich_request(db, request) -> RiskCheckRequest:
    """按 event_type 补全业务字段"""
    if request.event_type == "purchase":
        # 查订单关联的课程
        row = await db.execute(
            select(OrderInfo.course_id, OrderInfo.amount)
            .where(OrderInfo.order_id == request.source_id)
        ).first()
        if row:
            request.course_id = row.course_id
            # 把金额注入 event_data 供特征计算用
            request.event_data = request.event_data or {}
            request.event_data["amount"] = float(row.amount)
    elif request.event_type == "refund_apply":
        # 查退费申请的关联订单 + 已学时长
        # ...
    # register/login/real_name_auth 通常不需要补全
    return request

async def _check_all_blacklists(db, request) -> str | None:
    """扩展：4 类黑名单（用户/学号/身份证/设备指纹）"""
    # 1. 用户黑名单
    if await check_blacklist(db, "用户", request.user_id):
        return "用户"
    # 2. 学号黑名单（如果有 student_id）
    if request.event_data and request.event_data.get("student_id"):
        if await check_blacklist(db, "学号", request.event_data["student_id"]):
            return "学号"
    # 3. 设备指纹黑名单
    if request.device_id:
        if await check_blacklist(db, "设备指纹", request.device_id):
            return "设备指纹"
    # 4. 身份证黑名单（如果已实名）
    # ...
    return None
```

---

## 八、决策引擎核心（不动）

以下代码 **完全不变**（因为它是计算逻辑，跟业务无关）：

| 模块 | 文件 | 是否修改 | 原因 |
|------|------|---------|------|
| `calculate_final_score` | `decision.py` | **不动** | 评分公式 `max(hits)+BONUS×extra` 通用 |
| `_score_to_decision` | `decision.py` | **不动** | 4 档决策阈值通用 |
| `_score_to_level` | `decision.py` | **不动** | 风险等级映射通用 |
| `veto_check` | `decision.py` | **不动** | 一票否决通用 |
| `dual_track_fusion` | `decision.py` | **不动** | 规则+ML 融合通用 |
| `match_rules` | `rule.py` | **不动** | JSON 条件求值通用 |
| `evaluate_condition` | `rule.py` | **不动** | 递归求值通用 |
| `ml_model.py` | 引擎 | **不动** | XGBoost 加载/预测逻辑不变 |
| `scheduler.py` | 应用 | **不动** | 定时任务框架不变 |

**需要改的只有**：`decision.py` 里引用的事件类型文字常量（日志/注释），以及 `run_risk_check` 中第 7 步的 `_create_or_update_profile` 里对 `user_profile` 的字段写入要适配教育维度。

---

## 九、黑名单扩展（`app/models_risk.py:RiskBlacklist`）

```python
class RiskBlacklist(Base):
    """黑名单 — 教育版 type 枚举扩展"""
    __tablename__ = "risk_blacklist"

    # ...（其他字段不变）
    type: Mapped[str] = mapped_column(
        Enum("用户", "手机号", "地址",  # 保留电商原有的3类（地址在无物理邮寄的教育场景用得少）
             "学号", "身份证", "设备指纹", "支付账号",  # 新增4类教育专属
             name="blacklist_type_enum"),
        nullable=False, comment="黑名单类型",
    )
    # 优先级：用户 > 学号 > 身份证 > 设备指纹 > 支付账号 > 手机号
```

---

## 十、规则数据映射对照

你给的表 + 我补充的教育规则，对应电商规则库的完整替换：

| 编号 | 规则名 | event_type | rule_category | risk_level | action |
|------|--------|-----------|---------------|------------|--------|
| R001 | 刷单式报名 | purchase | 营销作弊 | 极高 | 拒绝 |
| R002 | 0学时退费 | refund_apply | 退费欺诈 | 高 | 人工审核 |
| R003 | 同一IP批量注册 | register | 注册风险 | 极高 | 拒绝 |
| R004 | 黑设备拦截 | register/login | 注册风险 | 极高 | 拒绝 |
| R005 | 大额连报 | purchase | 支付风险 | 高 | 人工审核 |
| R006 | 短时高频报名 | purchase | 账号风险 | 高 | 人工审核 |
| R007 | 账号盗用登录 | login | 账号风险 | 高 | 人工审核 |
| R008 | 假学员代理 | course_watch | 刷课行为 | 极高 | 拒绝 |
| R009 | 设备IP段异常 | register/purchase | 注册风险 | 高 | 人工审核 |
| R010 | 凌晨刷课 | course_watch | 刷课行为 | 中 | 标记 |
| R011 | 学完即退 | refund_apply | 退费欺诈 | 高 | 拒绝 |
| R012 | 退费连环 | refund_apply | 退费欺诈 | 中 | 标记 |
| R013 | 未实名报名 | purchase | 账号风险 | 中 | 标记 |
| R014 | 虚假邀请奖励 | coupon_claim | 营销作弊 | 高 | 拒绝 |
| R015 | 凌晨批量注册 | register | 注册风险 | 中 | 标记 |
| R016 | 未成年人保护 | purchase/course_watch | 账号风险 | 高 | 人工审核+通知监护人 |
| R017 | 内容违规投诉 | complaint | 内容安全 | 高 | 冻结课程 |
| R018 | 直播打赏异常 | purchase | 支付风险 | 中 | 标记 |
| R019 | 老师自买自评 | purchase | 师资风险 | 高 | 人工审核 |
| R020 | 退费投诉联动 | complaint/refund_apply | 内容安全 | 高 | 冻结课程 |
| R021 | 同一支付代付多人 | purchase | 支付风险 | 高 | 人工审核 |
| R022 | 宣传文案违规 | complaint(机构端) | 内容安全 | 中 | 标记+内容下架 |
| R023 | 预付费超限额 | purchase | 支付风险 | 中 | 标记+监管预警 |
| R024 | 新设备+老账号登录 | login | 账号风险 | 中 | 标记 |
| R025 | 学员身份不符 | purchase | 师资风险 | 中 | 标记 |
| R026 | 代付洗钱 | purchase | 支付风险 | 高 | 人工审核 |
| R027 | 刷单式试听 | coupon_claim | 营销作弊 | 中 | 标记 |
| R028 | 多设备同时看课 | course_watch | 刷课行为 | 高 | 人工审核 |
| R029 | 课程价格异常变更 | 通用 | 师资风险 | 高 | 人工审核 |
| R030 | 黑学号拦截 | 通用 | 账号风险 | 极高 | 拒绝 |

---

## 十一、改动总结

### 需要重写的文件（5 个）

| 文件 | 原因 |
|------|------|
| `app/models_business.py` | 10 张全新业务表 |
| `app/engine/feature.py` | 20 维教育特征 |
| `app/service/validator.py` | 教育实体校验 |
| `app/service/event.py` | 教育补全逻辑 |
| `app/schemas.py` | event_type + Literal 枚举 |

### 需要修改的文件（3 个）

| 文件 | 改动 |
|------|------|
| `app/models_risk.py` | event_type + rule_category + blacklist_type 枚举 |
| `app/engine/decision.py` | event_type 常量引用 + profile 字段 |
| `sql/init_risk_tables.sql` | 规则 INSERT 数据 |

### 完全不动（7 个）

| 文件 | 原因 |
|------|------|
| `app/engine/rule.py` | JSON 条件引擎通用 |
| `app/engine/ml_model.py` | XGBoost 加载/预测不变 |
| `app/engine/decision.py`（评分计算部分） | 公式通用 |
| `app/scheduler.py` | 定时任务框架不变 |
| `app/logging_config.py` | 日志配置不变 |
| `app/config.py` | 阈值配置（不改结构，调值可选） |
| `app/database.py` | ORM 基类 + 连接池不变 |