# P2 实施报告：XGBoost 与医疗管理端

## 交付结果

- 增加 numpy、pandas、scikit-learn、xgboost，由 uv 锁定。
- 完成 25 维严格顺序的模型向量、加载、推理、元数据校验和纯规则降级。
- 训练数据逐条调用线上 `compute_all_features`，标签采用教学型规则弱监督，规则结果不作为特征。
- 使用 60/20/20 分层拆分；独立校准集做 Platt scaling；输出 AUC、F1、阈值、正负比例和特征重要性 TOP 10。
- 双轨融合增加规则动作下限和融合后一票否决保护。
- 完成仪表盘、风险检查、规则、案件、评估和黑名单页面。

## 本地训练结果

- 样本：975；正例：324；负例：651；正例比例：0.3323。
- 训练：585；校准：195；验证：195。
- 验证 AUC：1.000；验证 F1：1.000；最佳阈值：0.35。
- 质量线：AUC ≥ 0.70、F1 ≥ 0.50，已通过。

指标是合成模式与弱监督标签上的教学验收结果，不作为真实医疗泛化结论。

## 验收

- 模型加载与纯规则降级均有自动测试。
- 真实 `CLM0000001 / PAT000001`：拒绝 99 分，MR001 + MR002，模型概率 1.000。
- 浏览器检查 6 个 P2 页面，无乱码、无脚本错误、无电商文案。
