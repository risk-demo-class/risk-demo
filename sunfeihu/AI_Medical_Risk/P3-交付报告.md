# P3 交付报告：AI 助手、告警、审计与自检

## 交付结果

- 只读 AI 助手支持风险总览、合成患者风险摘要、规则解释。
- 无 LLM Key 时本地查询回答；LLM 不可用时自动回退，不影响风控主流程。
- 拦截凭据/敏感标识请求、系统写操作、诊断/治疗/用药建议。
- 实现案件积压、高风险命中率、医生日处方金额、医院日医保申报金额告警。
- 定时任务间隔由 `.env` 控制；手工脚本可单次运行；相同未结指标不重复建告警。
- 规则启停、案件审核、黑名单变更和系统自动拒绝建案写入审计日志。
- 启动自检覆盖 Python、`.env`、依赖、MySQL、17 张表、模型、LLM 和端口。
- 完成 AI 助手、告警与审计页面及从零启动 README。

## 本地验收

- 告警检查：4 项，创建 2 条（24 小时高风险命中率、单医生日处方金额）。
- 无 Key 助手：正常返回当前评估、高风险和待处理案件聚合统计，并包含医疗安全声明。
- 审计：MR015 成对启停后生成 2 条日志，规则最终恢复原状态。
- 启动自检：关键项全部 PASS；LLM 未配置为 WARN，不阻断启动。
- 页面：8 个页面均实际浏览器验收，无控制台错误。
- 自动测试：33 passed，2 skipped；真实 MySQL：2 passed。
