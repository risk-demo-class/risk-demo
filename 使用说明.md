# 物流风控系统 · 使用说明

---

## 目录

1. [项目简介](#1-项目简介)
2. [环境准备](#2-环境准备)
3. [快速开始](#3-快速开始)
4. [启动 Web 服务](#4-启动-web-服务)
5. [前端演示](#5-前端演示)
6. [风险检查演示用例](#6-风险检查演示用例)
7. [数据脚本详解](#7-数据脚本详解)
8. [模型训练与验收](#8-模型训练与验收)
9. [运行测试](#9-运行测试)
10. [常见问题](#10-常见问题)

---

## 1. 项目简介

以「物」（包裹、重量、品类）为核心、寄件人与收件人双向风险的**物流风控系统**。
事件驱动式风控流水线：`事件 → 特征 → 规则 → 双轨融合(XGBoost + 规则) → 决策`。

**4 类物流事件**（`parcel_pickup` 揽收 / `dangerous_declare` 危险品申报 / `cross_border_ship` 跨境发运 / `cod_settlement` COD结算），
兼容保留 4 类电商事件（下单 / 支付 / 售后申请 / 物流投诉）。

**8 条物流规则**：

| 规则   | 名称                        | 风险等级     |
| ---- | ------------------------- | -------- |
| R001 | 未实名寄件 / 寄件黑地址             | 高        |
| R002 | 危险品瞒报（申报价偏低）              | 高        |
| R005 | 跨境违禁品（国际件申报危险品）           | 极高（一票否决） |
| R008 | COD 逾期卷款（金额 ≥ 1000）       | 极高（一票否决） |
| R012 | 同地址 24h 高频寄件 ≥ 3          | 中        |
| R018 | 收件人 ≥ 3 + 价值 ≥ 2000（改派异常） | 高        |
| R025 | 大额申报 ≥ 3000 + 单价偏低（低报）    | 高        |
| R030 | 命中黑地址 ≥ 1                 | 极高（一票否决） |

**决策阈值**：评分 < 30 通过 → < 60 标记 → < 80 人工审核 → ≥ 80 拒绝；ML 加载后 `final = 0.5×规则 + 0.5×ML`。

**25 维特征**：用户(寄件人)10 维 + 包裹 8 维 + 地址 7 维。

---

## 2. 环境准备

| 项      | 要求                                             |
| ------ | ---------------------------------------------- |
| Python | 3.11+（推荐 uv 管理虚拟环境 `.venv`）                    |
| MySQL  | 8.0+，本地账号，端口 3306，字符集 utf8mb4                  |
| 配置文件   | 项目根目录 `.env`（含 `DB_PASSWORD` 真实密码、XGBoost 开关等） |

```bash
# 安装依赖（推荐 uv）
uv sync
# 或: 从 requirements 安装
uv run python -m pip install -r requirements.txt
```

`.env` 关键项：

```ini
# MySQL 连接
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=你的真实密码
DB_NAME=ecs

# XGBoost（首次没模型时自动降级为纯规则，不影响业务）
XGB_ENABLED=True
```

---

## 3. 快速开始

### 方式 A：一条龙

```bash
uv run python scripts/one_command.py
```

自动执行 6 步：**重置数据库 → 造 24 个 RISK 风险用户 → 造训练数据 → 训练 XGBoost → 回填 ML 评分 → 启动 Web 服务**。

| 参数             | 说明                        |
| -------------- | ------------------------- |
| `--skip-init`  | 跳过 DB 重置 + RISK 用户（数据已就绪） |
| `--skip-train` | 跳过训练数据 + 模型训练 + 回填（模型已就绪） |
| `--only-start` | 只造今日业务数据并启动（日常演示）         |

### 方式 B：分步手动

```bash
# 1) 建库建表（26 脚本重建 + 业务种子数据 + 8 条物流规则）
uv run python scripts/init_db.py --reset --yes

# 2) 造 24 个高风险用户（7 种物流风险模式）
uv run python scripts/gen_risky_users.py

# 3) 造 800 条风控评估（正例 ~40%，覆盖 4 类物流事件）
uv run python scripts/gen_risk_data.py

# 4) 造带日期数据（仪表盘时间趋势，可选）
uv run python scripts/gen_risk_data_with_dates.py --days 3 --per-day 100

# 5) 造严格标注训练集（ml_score 强制 NULL，防旧模型污染标签）
uv run python scripts/gen_train_dataset.py

# 6) 训练真实模型（输出 val_auc / val_f1 / is_fake_convergence）
uv run python scripts/train_xgb_model.py

# 7) 回填 ML 评分（正负分离度 0.61）
uv run python scripts/backfill_ml_score.py

# 8) 造 10 万级大表（用户/包裹/申报/COD，可选）
uv run python scripts/gen_10w_data.py

# 9) 启动 Web 服务
uv run python run_app.py
```

---

## 4. 启动 Web 服务

```bash
uv run python run_app.py
```

启动前自动预检 6 项，全部应为 `[OK]`：

```
[OK] .env           .env 存在
[OK] Python 依赖      主要依赖已安装
[OK] MySQL 连接       MySQL root@ecs 连接正常
[OK] 数据库初始化       risk_rule 已有 8 条规则
[OK] 端口 8000        端口未被占用
[OK] XGBoost 模型     XGBoost 模型已加载
```

浏览器打开 **http://localhost:8000**（默认 `0.0.0.0:8000`，局域网可访问）。

---

## 5. 前端

| 页面    | 路由             | 要点                                   |
| ----- | -------------- | ------------------------------------ |
| 仪表盘   | `/`            | 今日评估数、高风险占比、决策分布、案件待办、7 天趋势          |
| 风险检查  | `/risk-check`  | 核心，见第 6 节风险检查                        |
| 评估历史  | `/assessments` | 按事件类型筛选（揽收/危险品/跨境/COD），查看规则 + ML 双评分 |
| 案件管理  | `/cases`       | 拒绝/审核命中自动生成案件，支持一键复核                 |
| 规则管理  | `/rules`       | 8 条物流规则，按分类筛选（寄递实名/危险品申报/跨境合规/代收货款…） |
| 黑名单   | `/blacklist`   | R030 黑地址管理                           |
| AI 助手 | `/chat`        | 自然语言查询包裹/用户/COD，如「查询 RISK001 的包裹」    |

风险检查页会按事件类型**动态切换来源输入框提示**：

| 事件类型                    | 来源输入提示    |
| ----------------------- | --------- |
| 揽收 parcel_pickup        | 包裹 ID     |
| 危险品申报 dangerous_declare | 申报 ID     |
| 跨境发运 cross_border_ship  | 包裹 ID     |
| COD结算 cod_settlement    | COD 流水 ID |

---

## 6. 风险检查

**实测结果**：

| #   | 事件类型  | 来源ID             | 用户ID      | 触发规则             | 决策     | 评分  | 等级  | ML    |
| --- | ----- | ---------------- | --------- | ---------------- | ------ | --- | --- | ----- |
| 1   | 揽收    | `P000001`        | `U0001`   | 无（正常对照）          | **通过** | 1   | 低   | 0.008 |
| 2   | 危险品申报 | `DCL_RISK002_01` | `RISK002` | R002 危险品瞒报       | **拒绝** | 84  | 极高  | 0.88  |
| 3   | COD结算 | `COD_RISK004_01` | `RISK004` | R008 COD卷款       | **拒绝** | 92  | 极高  | 0.81  |
| 4   | 跨境发运  | `P_RISK017_01`   | `RISK017` | R005 跨境违禁品（一票否决） | **拒绝** | 90  | 极高  | 0.59  |

---

## 7. 数据脚本

| 脚本                            | 作用                                   | 常用参数                                         |
| ----------------------------- | ------------------------------------ | -------------------------------------------- |
| `init_db.py`                  | 建库建表 + 种子数据 + 8 条规则                  | `--reset --yes` 全量重建                         |
| `gen_risky_users.py`          | 造 20-30 个 RISK 高风险用户（默认 24），7 模式轮换   | `--count` 指定数量                               |
| `gen_risk_data.py`            | 造 500-2000 条评估，正例 ≥15%，覆盖 4 事件类型     | `--balance-pos` 控正例比例                        |
| `gen_risk_data_with_dates.py` | 造带日期的评估（时间趋势用）                       | `--days 3 --per-day 100`、`--force-pos-ratio` |
| `gen_train_dataset.py`        | 造强标注训练集，`ml_score=NULL`（防旧模型污染）      | `--n-risk 24 --per-user 25`、`--dry-run`      |
| `train_demo_model.py`         | 教学模型（6 风险模式合成数据，快出结果）                | `--n 500`                                    |
| `train_xgb_model.py`          | 真实模型（用 DB 训练集，含假收敛检测）                | `--balance-pos` 重造数据                         |
| `backfill_ml_score.py`        | 用训练好的模型回填 `risk_assessment.ml_score` | —                                            |
| `gen_10w_data.py`             | 造 10 万级业务大表                          | —                                            |

**7 种物流风险模式**（gen_risky_users.py）：未实名寄件 / 危险品瞒报 / 跨境违禁品 / COD卷款 / 改派异常 / 大额低报 / 黑地址寄件。

---

## 8. 模型训练与验收

```bash
uv run python scripts/train_xgb_model.py
```

实测输出关键指标：

| 指标                  | 实测值    | 验收标准         | 结果  |
| ------------------- | ------ | ------------ | --- |
| val_auc             | 0.9348 | ≥ 0.7        | ✅   |
| val_f1              | 0.8599 | ≥ 0.5        | ✅   |
| is_fake_convergence | False  | False        | ✅   |
| best_iteration      | 50     | ≥ 30（warmup） | ✅   |

训练完成会输出特征重要性 TOP 10（如 `order_piece_count`、`user_real_name_verified`、`user_total_parcel_count_30d`），辅助特征工程迭代。

> 若训练后提示「假收敛嫌疑」（best_iter 过低 / val_auc 低），用 `--balance-pos` 重造数据再训。

**预训练模型**（demo）：

```bash
uv run python scripts/train_demo_model.py     # val_auc=1.0 / val_f1=0.99
```

---

## 9. 运行测试

```bash
uv run python -m pytest tests/ -k "not scheduler" -q
# 期望: 313 passed（≥200），跳过 1 个调度器测试
```

---

## 10. 常见问题

**Q1：`Access denied` / 数据库连接失败**
`.env` 里 `DB_PASSWORD` 是否与本地 MySQL 一致？预检用 `settings` 读 `.env`，不要把密码写死在代码里。

**Q2：启动后看不到「拒绝」决策**
数据正例比例不够，重跑 `gen_risk_data.py --balance-pos`；或先用 `gen_risky_users.py` 造 RISK 用户再跑。

**Q3：训练提示假收敛**
正例比例 < 15% 或特征无区分度。用 `--balance-pos` 重造数据；或调高 `XGB_WARMUP_ROUNDS`。

**Q4：Windows 终端中文乱码 / emoji 报错**
脚本已内置 `sys.stdout.reconfigure(encoding="utf-8")`；终端建议 `chcp 65001` 切 UTF-8。

**Q5：`--help` 报 `unsupported format character`**
argparse help 字符串里的裸 `%` 需写成 `%%`（本项目已处理）。

**Q6：`event loop is closed` 警告**
Windows 下 ad-hoc 脚本用完 `async_engine.dispose()` 即可；项目自带脚本已处理，不影响业务。
