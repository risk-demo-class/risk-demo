# 教育风控源码框架迁移：完成计划

## 目标

在不改动 `D:\PythonProject\AI_Risk` 的前提下，沿用其 Router、Service、Engine、Model 与运营页面的分层方式，完成教育风控的可演示运营闭环。当前阶段继续使用已有 SQLite 演示库；Docker/MySQL 与模型训练等待后续单独接入。

## 已完成

- 报名风险检查、规则查询、案件列表、评估历史已按教育业务落地。
- 基础模板导航和四个运营页面已迁入教育项目。

## 执行顺序

### 切片 1：教育黑名单管理闭环

**范围：** `RiskBlacklist` 的分页查询、新增、删除 API 与 `/blacklist` 运营页面。

**验收：**
- `GET /api/blacklist` 返回教育黑名单分页数据；支持按类型筛选。
- 可新增、删除学员账号、学号/证件哈希、设备指纹三类黑名单。
- `/blacklist` 不再 404，且只展示脱敏或哈希值。
- 新行为先有失败测试，完整测试通过后单独提交。

### 切片 2：真实教育仪表盘

**范围：** 迁移旧项目的 `/api/dashboard/overview` 契约，用已有评估、事件、案件、规则命中数据实时汇总；首页接入该接口。

**验收：**
- 显示今日评估、高风险、待审核案件、通过率。
- 显示近七日评估趋势与规则命中 TOP5；无数据时有稳定的空状态。
- 保持旧项目页面结构和教育术语，完整测试通过后单独提交。

### 切片 3：案件审核闭环

**范围：** 为已有 `RiskCase` 增加单案件详情和审核状态更新，页面提供待审核、已通过、已拒绝的教育风控处理入口。

**验收：**
- 仅允许定义好的审核状态流转。
- 审核结果可回查，且不会改写原始评估与规则命中快照。
- 新行为先有失败测试，完整测试通过后单独提交。

### 切片 4：教育多场景风险引擎

**范围：** 在既有七步引擎上依次接入退款申请、学历/身份认证两个已有教育业务实体及对应特征和规则；不接入电商业务。

**验收：**
- 报名、退款、认证均生成事件、特征、评估和必要案件。
- 黑名单拦截和可解释规则命中继续可回查。
- 每个场景独立测试、独立提交。

### 切片 5：最终质量检查

**验收：**
- 页面中没有电商术语与无效导航。
- 全量测试通过；对页面做一次实际浏览器运行验证。
- Docker/MySQL、XGBoost 教育样本训练作为下一阶段，不提前伪造实现。

## 约束与风险

- 不删除遗留静态演示页，除非用户明确确认删除；当前 Jinja 运营页面是实际入口。
- 旧项目仅作结构和交互参照；教育规则、特征和训练数据必须使用教育语义。
- 数据库迁移前不更改为 MySQL 配置，避免误连用户本机或旧电商库。
