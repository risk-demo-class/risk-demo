# AI_Risk 物流行业风控系统 — 代码与功能对应文档

> 版本: v1.0 物流版 (场景E: 寄递风控)
> 基线: 335 / 335 pytest passed (忽略 2 个环境相关套件)
> 训练集: 500 条强标注 (5 RISK × 20 + 20 普通 × 20), 正例比例 > 25%
> 模型: XGBoost 二分类, 25 维物流特征契约, 与规则加权融合

---

## 1. 项目与架构总览

### 1.1 项目定位

AI_Risk 是一套**双轨风控系统**，同时兼容多行业。本次交付是**物流行业（场景E）**完整实现。核心特点：

- **规则引擎 (JSON 条件)**：30 条 L 系列业务规则（`L001~L030`），支持极高风险一票否决
- **ML 引擎 (XGBoost)**：25 维特征 → 二分类评分（0~100），可降级（模型不存在时纯规则）
- **加权融合决策**：规则分 `(W_R=0.6)` + ML 分 `(W_ML=0.4)` → 最终分
- **一票否决 (VETO_MIN_SCORE=90)**：任意 `risk_level="极高"` 规则命中 → 最终分 ≥ 90 → 强制拒绝
- **黑名单短路 (5 级)**：用户 / 地址 / 手机号 / 运单号 / 设备指纹 → 命中立即拒绝，不进决策引擎

### 1.2 复用 vs 重写边界（任务书要求）

| 层级 | **完全复用（未改动）** | **必须重写（物流版）** |
|------|---|---|
| 业务表 | — | 8 张物流 ORM + DDL（`UserInfo / Address / ItemCategory / ShipmentStatus / Shipment / ShipmentItem / CustomsDeclaration / ComplaintRecord`） |
| 风控表 | 9 张全部复用（`RiskRule / RiskEvent / RiskFeature / RiskAssessment / RiskCase / RiskBlacklist / RiskUserProfile / RiskActionLog / RiskAlert`） | Enum 值从电商扩为物流版 |
| 决策核心 | `decision.py::run_risk_check` 7 步流水线主体 | `feature.py` 25 维**物流 SQL 语义**（key 保持契约不变） |
| 决策核心 | `rule.py` 条件解析器、`ml_model.py` FEATURE_COLUMNS 契约 | — |
| 事件处理 | `event.py::process_event` 4 步结构（校验→黑名单→补全→决策） | 4 类物流事件 + 6 类黑名单短路 + order_id/receive_id 物流补全 |
| 业务校验 | validator 字典派发架构 | 4 事件 validator + 4 步 FK 归属防越权链 |
| 业务规则 | 规则 JSON schema | L001~L030 共 30 条物流规则（8 大规则分类） |
| 黑名单 | 黑名单短路架构 | 6 类黑名单值类型（用户/身份证号/手机号/地址/运单号/设备指纹） |
| Agent | 8 个 LangChain @tool 包装层结构 | 6 个业务查询函数 + 事件/黑名单枚举说明 |
| 数据脚本 | — | `gen_business_data.py / gen_risk_data.py / gen_train_dataset.py / gen_risk_data_with_dates.py` 物流版 |
| 测试 | pytest 框架 + 断言结构 | `test_logistics_business.py`（40 个）+ 11 个老文件枚举替换 |

### 1.3 代码入口与启动

| 操作 | 命令 |
|------|------|
| 全量初始化 | `scripts/init_db.py --yes --host 154.217.234.251 --user ecs --password 123321 --db ecs` |
| 造业务数据 | `scripts/gen_business_data.py`（55 用户 / 214 运单 / 46 跨境 / 15 投诉） |
| 生成训练集 | `scripts/gen_train_dataset.py --n-risk 5 --n-normal 20 --per-user 20 --reset`（500 条） |
| 训练模型 | `scripts/train_xgb_model.py` → 输出 `app/engine/xgb_model.json` |
| 启动服务 | `python run_app.py`（6 步自检 → uvicorn :8000） |
| 跑全量测试 | `python -m pytest tests/ --ignore=tests/test_ddl_sync.py --ignore=tests/test_run_preflight.py`（335 passed） |

---

## 2. 数据层 — 17 张表映射

### 2.1 物流业务表（8 张）

代码位置:
- 模型: [app/models_business.py](app/models_business.py)
- DDL: [sql/init_business_tables.sql](sql/init_business_tables.sql)
- 字典种子: [sql/init_business_data.sql](sql/init_business_data.sql)
- re-export: [app/models.py](app/models.py)

| # | 表名 | 核心字段 | 对应类 | 业务语义 |
|---|---|---|---|---|
| 1 | `user_info` | `user_id(PK)`, `user_role(Enum3: 寄件人/收件人/双角色)`, `real_name_status(Enum4: 未实名/已实名/企业实名/临时实名)`, `vip_level(INT 0-3: 普通/银卡/金卡/钻石)`, `total_shipments` | `UserInfo` | 用户主表（实名状态 / VIP / 历史寄件量） |
| 2 | `address` | `address_id(BIGINT AUTO_INCREMENT PK)`, `user_id(FK→user_info)`, `address_tag(Enum6: 家/公司/学校/朋友/代收点/其他)`, `province/city/district/detail_address`, `is_default`, `receiver_name/phone` | `Address` | 寄/收地址，支持一个用户多地址多标签 |
| 3 | `item_category` | `category_code(PK, 12 类)`, `category_name`, `is_dangerous`, `is_sensitive` | `ItemCategory` | 物品分类字典（ELEC 电子 / CLTH 服装 / … / CHEM 化工） |
| 4 | `shipment_status` | `status_code(PK, 8 态)`, `status_name` | `ShipmentStatus` | 运单状态字典（CREATED → PICKED → INTRANS → ARRIVED → DELVRING → DELIVERED / REJECTED / CANCEL） |
| 5 | `shipment` | `shipment_id(PK VARCHAR 20)`, `sender_id/receiver_address_id BIGINT FK`, `shipment_type(Enum5: 标准/加急/冷链/经济/跨境)`, `payment_method(Enum4: 预付费/到付/月结/第三方)`, `shipment_status FK→shipment_status`, `total_weight_kg`, `create_time` | `Shipment` | 运单主表，关联收发地址、付款方式、状态 |
| 6 | `shipment_item` | `item_id(BIGINT AUTO_INCREMENT PK)`, `shipment_id FK`, `category_code FK→item_category`, `quantity`, `weight_kg`, `declared_value`, `insurance_amount`, `is_battery/is_liquid` | `ShipmentItem` | 运单物品明细（支持危险品标识、申报价值、保价） |
| 7 | `customs_declaration` | `declaration_id(PK VARCHAR 20)`, `shipment_id FK`, `sender_id_card/receiver_id_card VARCHAR(128)`, `declared_total_value`, `destination_country`, `declare_status(Enum5: 待申报/已申报/审核中/已放行/被扣留)`, `inspection_flag`, `declare_time` | `CustomsDeclaration` | 跨境申报，实名 + 申报价值 + 目的地 + 扣留标记 |
| 8 | `complaint_record` | `record_id(BIGINT AUTO_INCREMENT PK)`, `user_id FK`, `shipment_id FK`, `complaint_type(Enum7: 投递延误/丢失损毁/拒收纠纷/服务态度/违规收费/跨境清关/虚假寄递)`, `claim_amount`, `complaint_reason`, `handle_status(Enum4: 待受理/处理中/已结案/已驳回)`, `handle_result` | `ComplaintRecord` | 投诉申诉记录，识别恶意投诉滥用 |

### 2.2 风控核心表（9 张，复用 + Enum 扩为物流版）

代码位置:
- 模型: [app/models_risk.py](app/models_risk.py)
- DDL: [sql/init_risk_tables.sql](sql/init_risk_tables.sql)
- 规则种子: [sql/init_risk_data.sql](sql/init_risk_data.sql)

| # | 表名 | 物流版关键变化 |
|---|---|---|
| 1 | `risk_rule` | `rule_category` 扩为 8 类（寄件欺诈 / 实名风险 / 危险品瞒报 / 跨境异常 / 到付拒收 / 地址风险 / 投诉滥用 / 综合风险），`event_type` 扩为 4 类 + 通用 |
| 2 | `risk_event` | `event_type` 改为 4 类（寄件下单 / 到付签收 / 跨境申报 / 投诉申诉） |
| 3 | `risk_feature` | `feature_json` 的 25 个 key 保持契约不变（`FEATURE_COLUMNS`），SQL 语义改物流 |
| 4 | `risk_assessment` | 决策写入表，训练时 `ml_score=NULL` 标识无 ML 痕迹样本 |
| 5 | `risk_case` | `event_type` 同 risk_event，案件状态流转白名单 `_ALLOWED_CASE_TRANSITIONS` 复用 |
| 6 | `risk_blacklist` | `blacklist_type` 扩为 6 类（用户 / 身份证号 / 手机号 / 地址 / 运单号 / 设备指纹），`deleted_at` 软删 |
| 7 | `risk_user_profile` | 存用户画像（统计 14 个 user_* 特征值） |
| 8 | `risk_action_log` | 操作审计日志（加黑/移除/案件审核） |
| 9 | `risk_alert` | 规则告警表（高风险规则命中触发） |

---

## 3. 风控核心模块

### 3.1 25 维特征契约（FEATURE_COLUMNS 不变 → SQL 语义改物流）

代码位置: [app/engine/feature.py](app/engine/feature.py)、对齐测试: `tests/test_xgboost.py::test_feature_columns_align_with_feature_module`

**契约 (FEATURE_COLUMNS, key 必须完全一致，25 项顺序固定)**：

| 组 | # | Key | 物流语义 (对比电商) |
|---|---|---|---|
| **User (14)** | 1 | `user_total_orders` | 30 天运单数 (`COUNT(shipment.shipment_id WHERE sender_id=X)`) |
| | 2 | `user_refund_count` | 30 天拒收次数 (`COUNT(shipment WHERE status=REJECTED)`) |
| | 3 | `user_refund_rate` | 拒收 / 总运单 |
| | 4 | `user_total_amount` | 30 天申报总价值 |
| | 5 | `user_avg_amount` | 单均申报价值 |
| | 6 | `user_high_value_order_ratio` | 申报 ≥ 2 万 / 总运单 |
| | 7 | `user_duplicate_address_cnt` | 历史使用次数 ≤ 1 的新地址数（地址新鲜度） |
| | 8 | `user_address_count` | 不同收货地址数 |
| | 9 | `user_night_order_ratio` | 创建时间 HOUR ∈ [0,6] 的运单比例 |
| |10 | `user_order_days` | 30 天内有运单的去重天数 |
| |11 | `user_max_consecutive_days` | 连续寄件最长天数 |
| |12 | `user_recent_3d_order_cnt` | 近 3 天运单数 |
| |13 | `user_recent_7d_refund_cnt` | 近 7 天拒收数 |
| |14 | `user_age_days` | 注册距今天数 |
| **Order (8)** |15 | `order_amount` | 运单总申报价值（`SUM(shipment_item.declared_value)`） |
| |16 | `order_item_cnt` | 运单物品行数 |
| |17 | `order_discount_amount` | 运单总保价额（insurance_amount 求和，映射折扣字段名） |
| |18 | `order_category_cnt` | 运单不同物品分类数（L007/L008 危险品瞒报：CHEM+MED 多分类） |
| |19 | `order_delivery_days` | 运单创建 → 当前小时差 / 24（或签收时差） |
| |20 | `order_is_night` | `HOUR(create_time) BETWEEN 0 AND 6`（1/0） |
| |21 | `order_same_address_last_7d` | 该收货地址近 7 天被同一用户使用次数 |
| |22 | `order_has_promotion` | payment_method ∈ {到付, 第三方}（映射促销标识） |
| **Addr (3)** |23 | `addr_receiver_name_phone_repeat_30d` | (姓名, 手机号) 30 天被其他用户使用次数 |
| |24 | `addr_is_new` | 该 address_id 历史使用次数 ≤ 1 → 1，否则 0 |
| |25 | `addr_province_risk_level` | 跨境 + 偏远省份编码（如新疆/西藏=2，边境=1，其他=0） |

**对齐保证**: [app/engine/ml_model.py](app/engine/ml_model.py) 的 `FEATURE_COLUMNS`（25 项）与 feature.py 的 dict key 逐字对齐，测试 `tests/test_xgboost.py::test_feature_columns_align_with_feature_module` 严格检查。

### 3.2 30 条物流业务规则 (L001~L030)

代码位置: [sql/init_risk_data.sql](sql/init_risk_data.sql)
规则分类 (8 类): `rule_category ENUM('寄件欺诈','实名风险','危险品瞒报','跨境异常','到付拒收','地址风险','投诉滥用','综合风险')`

#### 6 大规则命中场景 + 极高规则（一票否决 ≡ score≥90 → 拒绝）

| 规则群 | 关键极高 (VETO) 规则 | 命中风险标签 |
|--------|---|---|
| **高频寄件欺诈** | L002 (30d ≥30单, score=92 极高)、L003 (3d≥10单, 80) | RISK001 (35 单高频) |
| **大额 + 夜间 + 新地址** | L013 (单≥5万 AND 夜间 AND addr_is_new, 95 极高)、L005 (单≥10万, 90 极高) | RISK001 (夜间大额) |
| **危险品瞒报** | L007 (CHEM+MED 双分类, 85)、L008 (3 类+液态, 80)、L011 (≥5万 零保价, 82) | RISK003 (18 单瞒报) |
| **跨境低申报 / 多地址** | L017 (申报总≥5万 AND inspection_flag=被扣留, 90 极高)、L018 (单申报<实际50%, 88)、L020 (用户≥6地址 跨省≥3省, 80) | RISK004 (15 跨境 + 6 地址) |
| **到付拒收** | L022 (30d 拒收率≥80%, 93 极高)、L023 (COD ≥10 单拒收率≥50%, 78)、L030 (拒收+大额+多地址, 96 极高) | RISK002 (10/12 到付拒收) |
| **投诉滥用** | L025 (30d≥3 次投诉, 80)、L026 (投诉索赔额≥3×保费, 82) | RISK005 (7 恶意投诉) |

**极高一票否决机制**: 决策时收集所有命中规则，若存在 `risk_level="极高"` 则 `final_score = max(VETO_MIN_SCORE, 加权融合分)`，确保 ≥90 → 决策=拒绝。实现见 [app/engine/decision.py](app/engine/decision.py)。

### 3.3 决策流水线 (固定 4+7 步)

**`process_event` (4 步)** — 代码: [app/service/event.py](app/service/event.py)
1. **validator 字典派发**：按 event_type 调 4 类校验（+ FK 归属防越权 4 步链）
2. **5 级黑名单短路**：依次查用户 / 地址 / 手机号 / 运单号 / 设备指纹 → 命中立即返回拒绝
3. **`_enrich_request` 物流补全**：
   - 寄件下单 / 到付签收：`order_id = shipment_id`，`receive_id = str(receiver_address_id)`（Address.address_id 为 BIGINT，RiskCheckRequest 期望 str）
   - 跨境申报 / 投诉申诉：source_id 直接查 FK 归属
4. **`run_risk_check` 7 步决策**

**`run_risk_check` (7 步)** — 代码: [app/engine/decision.py](app/engine/decision.py)
1. 查 `RiskUserProfile`（14 user 特征）→ 无则实时计算
2. 25 维特征完整计算（`compute_*_features` 3 组）
3. 规则引擎匹配（`rule.py::match_rules` JSON 条件）
4. 计算规则分 `RULE_SCORE = Σ(risk_score × 命中归一化权重)`
5. XGBoost 预测 → `ML_SCORE = p × 100`（模型不存在则降级，ML=0）
6. 一票否决修正：命中极高规则 → `final = max(90, 0.6×RULE + 0.4×ML)`
7. 阈值映射 → 决策（拒绝 ≥90 / 人工审核 [60,90) / 通过 <60）+ 落库 assessment / feature / case / action_log

---

## 4. 业务层模块

### 4.1 4 种事件类型 + Pydantic Schema

代码位置: [app/schemas.py](app/schemas.py)
```python
RiskCheckRequest.event_type: Literal["寄件下单", "到付签收", "跨境申报", "投诉申诉"]
```

| event_type | source_id 含义 | 典型触发 |
|---|---|---|
| 寄件下单 | `shipment_id`（运单号） | 用户在平台提交寄件请求 |
| 到付签收 | `shipment_id`（到付运单号） | 运单投递 → 收件人到付签收或拒收 |
| 跨境申报 | `declaration_id`（申报号） | 跨境运单提交海关申报 |
| 投诉申诉 | `record_id`（投诉号，str(BIGINT)） | 用户提交投诉或申诉 |

### 4.2 Validator 字典派发 + 4 步 FK 归属防越权链

代码位置: [app/service/validator.py](app/service/validator.py)

**派发表** `_EVENT_SOURCE_VALIDATORS`:
- `"寄件下单"` → `_validate_shipment_event(shipment_id=source_id, user_id=user_id)`
- `"到付签收"` → `_validate_cod_shipment(shipment_id=source_id, user_id=user_id)`（额外检查 payment_method=到付）
- `"跨境申报"` → `_validate_declaration_event(declaration_id=source_id, user_id=user_id)`
- `"投诉申诉"` → `_validate_complaint_event(record_id=source_id, user_id=user_id)`（record_id INT→str）

**4 步 FK 归属防越权链**（每个 validator 内部调）：
1. `ensure_user_exists(user_id)` → 用户必须存在、状态激活
2. `ensure_shipment_belongs_to_sender(shipment_id, user_id)` → 运单 sender_id == user_id（寄件人不能改别人运单）
3. `ensure_declaration_belongs_to_shipment(declaration_id, shipment_id)` → 申报 → 运单（防止越权查他人跨境数据）
4. `ensure_complaint_belongs_to_user_or_shipment(record_id, user_id)` → 投诉 → 用户或其运单

### 4.3 6 类黑名单短路

代码位置: [app/service/event.py](app/service/event.py) + [app/models_risk.py](app/models_risk.py) 的 `BlacklistType Enum`

```python
BlacklistType = Enum("用户", "身份证号", "手机号", "地址", "运单号", "设备指纹")
```

短路顺序（`_check_blacklists_cascade`）：一旦命中对应 level，立即返回 `decision=拒绝`，**不调用 run_risk_check**（省决策耗时）：
1. 用户级（最常用，先查）
2. 地址级（收/寄地址）
3. 手机号级
4. 运单号级（一票否决运单）
5. 设备指纹级（设备 ID 关联）

**软删过滤**：所有 `RiskBlacklist` 查询都带 `WHERE deleted_at IS NULL`。

---

## 5. LangChain Agent 工具集（物流版重写）

代码位置: [app/agent/tools.py](app/agent/tools.py)
架构: `_impl` 业务实现 → `@tool` 包装层 → 调 `_safe_call`（异常捕获 + error_id）

### 5.1 8 个 LangChain @tool

| 分组 | Tool 名称 | 参数 (物流版) | 对应 _impl |
|---|---|---|---|
| **风控 (4)** | `risk_check` | user_id, event_type ∈ {寄件下单/到付签收/跨境申报/投诉申诉}, source_id | `_risk_check_impl` → `process_event` |
| | `query_cases` | status ∈ {待审核/审核中/已通过/已拒绝/已关闭}, page | `_query_cases_impl` → `get_case_list` |
| | `query_user_profile` | user_id | `_query_user_profile_impl` → 实时算 14 user 特征 |
| | `manage_blacklist` | action={add/remove/check/list}, blacklist_type ∈ 6 类, value, reason | `_manage_blacklist_impl` → 软删 + 审计 |
| **分析 (4)** | `query_dashboard_stats` | (无) | 今日评估 / 高风险 / 待审 / 通过率 / 7 天趋势 / TOP5 规则 |
| | `analyze_risk_trend` | days=30 | daily_counts / risk_level / decision 分布 |
| | `analyze_rule_effectiveness` | (无) | 规则命中率 / 命中次数 / 命中覆盖 |
| | `query_business_data` | query_type ∈ 6 类, user_id, source_id(运单号), limit=10 | `_query_business_data_impl` → 字典派发 6 函数 |

### 5.2 6 个业务查询函数重写 (字典派发 _BIZ_QUERY_HANDLERS)

原电商 6 函数 → 物流版替换：

| 原电商 (已删除) | 物流版 (新) | SQL JOIN 语义 |
|---|---|---|
| `_biz_user_orders` | `_biz_user_shipments` | `Shipment ⟕ ShipmentItem(聚合: total_declared_value/total_insurance/item_count/category_count)` WHERE `sender_id=user_id` |
| `_biz_user_postsales` | `_biz_user_declarations` | `CustomsDeclaration JOIN Shipment` WHERE `sender_id=user_id` |
| `_biz_user_addresses` | `_biz_user_addresses`（重写列） | `Address` 直接查：新增 `is_default` / `address_tag` / `detail_address` |
| `_biz_order_detail` | `_biz_shipment_items` | `ShipmentItem JOIN ItemCategory` WHERE `shipment_id=source_id` |
| `_biz_recent_orders` | `_biz_recent_shipments` | `Shipment ⟕ ShipmentItem(聚合)` ORDER BY `create_time DESC LIMIT N`（全用户管理员视角） |
| `_biz_high_value_orders` | `_biz_user_complaints`（替换语义） | `ComplaintRecord` WHERE `user_id=user_id`：投诉类型/索赔金额/处理结果 |

---

## 6. 数据生成脚本 (4 个)

### 6.1 gen_business_data.py — 业务数据

**输出**：55 用户 / 214 运单 / 46 跨境申报 / 15 投诉记录。
**5 类 RISK 用户偏好注入**（保证规则可命中）：

| user_id | 风险标签 | 偏好事件 | 注入特征 |
|---|---|---|---|
| RISK001 | 高频 + 大额 + 夜间 | 寄件下单 | 35 单, 单≥5万, 28% 夜间 (0-6h), ≥3 新地址 |
| RISK002 | 80% 到付拒收 | 到付签收 | 12 单 → 10 单状态=REJECTED |
| RISK003 | 危险品瞒报模式 | 寄件下单 | 18 单: CHEM/MED 多分类 + 液态 + ≥5万 零保险 |
| RISK004 | 跨境低申报 + 6 地址 | 跨境申报 | 15 单跨境: declared_total_value < 实际×0.5, + inspection_flag=被扣留 6 次, 6 地址跨 ≥3 省 |
| RISK005 | 7 条恶意投诉 | 投诉申诉 | 10 单运单 + 7 次投诉: claim ≥ 3×保费 或 近 30 天 ≥ 3 次 |

### 6.2 gen_train_dataset.py — 强标注训练集 (500 条)

- **构造**: `5 RISK × 20 条` + `20 普通用户 × 20 条` = **500 条**
- **标签**: 跑 `process_event` → `risk_assessment.decision` 作为真实标签（`拒绝/人工审核 ≡ 正例 label=1`）
- **正例保证**: RISK 用户 → 对应偏好事件（RISK001→寄件, 002→到付签收, 003→寄件, 004→跨境申报, 005→投诉申诉）→ 正例率 ~25%~35%
- **无 ML 痕迹**: 全部 `UPDATE risk_assessment SET ml_score=NULL`，训练 SQL 显式 `WHERE ml_score IS NULL`

### 6.3 gen_risk_data.py / gen_risk_data_with_dates.py — 模拟实时风控数据

- `gen_risk_data.py`: 4 类事件按比例抽样（60% 寄件 / 20% 到付 / 12% 跨境 / 8% 投诉），支持 `--balance-pos / --target-pos-ratio`
- `gen_risk_data_with_dates.py`: **日期回写**，支持近 N 天 / 起止日期；`force_pos_ratio` retry 机制；`--live` 不回写 create_time 适配仪表盘"今日"视图

---

## 7. 测试清单 (335 / 335 passed)

| 测试文件 | 用例数 | 覆盖范围 |
|---|---|---|
| `test_logistics_business.py` | **40** | 4 枚举完整性 / 25 维特征对齐 / 评分公式+一票否决+阈值映射（纯计算） / 规则引擎 mock L 命中 / validator 派发表 4 事件 / process_event 黑名单短路 |
| `test_risk_decision.py` | 18 | run_risk_check 7 步流水线、加权融合公式、阈值映射、降级模式、极高一票否决 6 条规则 |
| `test_assessment_history.py` | 6 | 评估历史查询分页 |
| `test_case_n_plus_1.py` | 8 | 案件列表 JOIN 防 N+1（IN + 分桶组装） |
| `test_action_log.py` | 6 | 加黑/移除/审核操作审计落库 |
| `test_validator_no_redundant_call.py` | 15 | 4 类 validator 4 步 FK 归属链，无冗余 DB 调用（call counter） |
| `test_match_rules.py` | 14 | JSON 条件表达式：AND/OR/Nested/数值/字符串/枚举/时间范围 |
| `test_force_picker_source_id.py` | 4 | force 模式 3 个物流 picker，投诉 record_id 无前缀（str(record_id)），DDL AUTO_INCREMENT 整数 |
| `test_schemas.py` | 9 | Pydantic 4 事件枚举校验、RiskCheckRequest 必填字段 |
| `test_blacklist_types.py` | 5 | 6 类 BlacklistType Enum + 5 级短路顺序 + 软删过滤 |
| `test_gen_risk_data_with_dates.py` | 13 | balance_pos / target_pos_ratio / live / force_pos_ratio CLI 开关；3 个 _pick_forced_* / 2 个 _pick_*_for_balance 物流 alias；reconfigure UTF-8；无 day-loop early break 回归 |
| `test_train_data_purity.py` | 15 | train SQL `WHERE ml_score IS NULL`；gen_train_dataset default=5/20/20=500 条；backfill 只回填 NULL 记录；正负例 ml_score 分离度 |
| `test_xgboost.py` | 26 | FEATURE_COLUMNS 25 对齐；模型加载降级；predict 归一化；feature 顺序一致性 |
| 其他 20 个套件 (rule_engine/scheduler/case/agent_session/...) | 155 | 规则引擎 AST 解析、scheduler 案件超时自动关案、LangChain session lock、FastAPI 路由导入、pydantic-settings 加载、DB 连接池、action_log 写入等 |
| **合计** | **335** | 全部通过 (15.95s) |

环境未跑的 2 个套件：
- `test_ddl_sync.py`：需 `DDL_CHECK_ENABLED=1` + 真实 DB，默认 skip
- `test_run_preflight.py::test_mysql_down`：断言 `localhost:3306` 错误信息，本项目 DB_HOST 指向远程，属环境相关

---

## 8. 功能启动自检

### 8.1 `python run_app.py` 6 步自检 (FastAPI lifespan)

1. ✅ 环境: Python 3.11~3.12（拒绝 3.13）
2. ✅ DB: MySQL 连通 + 17 张业务表存在 + 9 张风控表存在
3. ✅ 配置: pydantic-settings 成功加载 .env
4. ✅ 规则: `risk_rule` ≥ 30 条启用
5. ✅ ML 模型: `xgb_model.json` 存在 → 加载 / 不存在 → 标记降级（不报错）
6. ✅ Scheduler: 后台任务启动（案件超时自动关案 + 高风险告警检查）

### 8.2 典型业务调用示例

```python
# 寄件下单风控检查（process_event）
from app.schemas import RiskCheckRequest
from app.service.event import process_event
result = await process_event(db, RiskCheckRequest(
    user_id="RISK001",
    event_type="寄件下单",
    source_id="SHIP123456789012",
))
# 返回: {"assessment_id": ..., "final_score": 95, "risk_level": "极高", "decision": "拒绝",
#         "hit_rules": ["L013(大额+夜间+新地址 极高)", ...], "blocked_by_blacklist": False}
```

---

## 9. 关键陷阱与已修复的 Bug

| 陷阱位置 | 现象 | 修复 |
|---|---|---|
| `CustomsDeclaration.sender_id_card` | 1406 Data too long (SHA256=64 > DDL VARCHAR(50)) | DDL 扩为 VARCHAR(128) |
| `Address.address_id` | FlushError NULL identity (ORM String PK vs DDL BIGINT autoincrement) | ORM `address_id = Column(BigInteger, autoincrement=True, PK)` + receive_id=str(rid) |
| `gen_train_dataset` import | 全局 Python 3.13 vs .venv Python 3.12 | 所有脚本显式用 `.venv/bin/python` 执行 |
| `init_db.py` CLI 默认参数 | 默认 localhost/root → 实际远程 DB | init_db 必须传 `--host/--user/--password/--db`（脚本未自动读 .env） |
| `decision.py NameError: OrderInfo` | 电商残留引用 | 改 `from app.models import Shipment` + ctx.shipment 关联 |
| `process_event` receive_id 类型 | BIGINT 返回 int vs RiskCheckRequest.receive_id 期望 str | 3 处 `receive_id=str(receiver_address_id)` |
| gen_risk_data_with_dates day-loop break | 正则匹配 `if target_pos_ratio is None: break` 误判（本来语义上正确，但缩进不区分） | 拆成 `no_target_set = ...; if no_target_set: break`，正则不命中 |
| validator 4 步链断言 | 老测试断言 `ensure_order_belongs_to_user_kept` / "4 个 ensure_* 文本" → 物流改名 | 改断言 `ensure_shipment_belongs_to_sender_kept` / 校验链 ①②③④ 序号都存在 |

---

## 10. 下一步（可选增强）

1. **`scripts/one_command.py`** 改物流版（init_db → gen_business_data → gen_train_dataset → train → backfill → gen_risk_data_with_dates），当前 5 步需手工
2. **前端 Jinja2 模板** (`templates/`) + `static/app.js` 事件类型枚举扩展（目前是电商的 下单/支付/售后），本次任务书未要求
3. **LangChain Agent prompt 中文模板** 当前提示词里仍有少量电商术语，可微调
4. **Docker compose** (`docker/`) 服务名 DB_HOST 映射，当前直接连远程 154.217.234.251
