# Goal 1：建立基线并完成银行业务理解

复制下面整个代码块，作为一条消息发送给 Codex。

```text
/goal 在当前工作区建立一个可回滚、可审计的银行风控重构项目基线，并完成任务书“任务 1：业务理解”及银行风控前端设计简报。持续推进，直到业务说明、重构契约、前端设计简报、基线验证、本地 Git 和新建的私有 GitHub 仓库全部达到下述验收条件；不得开始数据层或前端代码改造。

工作区与输入：
- 目标工作区：D:\Program Files\temp_data\Projects\SGG_projects\risk-control
- 只读基线：D:\Program Files\temp_data\Projects\SGG_projects\sgg_AI_RISK_demo
- 任务书：D:\Program Files\temp_data\尚硅谷大模型项目之风控系统\AI_Risk_行业风控实战任务书.md
- 基线中的未跟踪文件 `1-业务说明.md` 是银行方向草稿，只能作为待审校素材，不能无检查地覆盖最终文档。
- 必须保留目标工作区现有的 `PROMPTS_README.md` 和 `prompts/` 目录。

授权边界：
- 可以读取基线、任务书和当前工作区，联网查询官方资料，在目标工作区写文件、运行非破坏性检查、初始化本地 Git、提交，并创建及推送一个新的私有 GitHub 仓库。
- GitHub 默认仓库名为 `ai-risk-bank`，可见性必须为 `private`。优先使用已连接的 GitHub 能力；覆盖不足时可使用已登录的 `gh` CLI。
- 在创建仓库前，先确认当前绑定的 GitHub 账户，并确认同名仓库不存在。如果已存在，停止并向用户报告，绝不覆盖、改名或复用。
- 不得修改只读基线，不得复制它的 `.git`、`.env`、缓存、日志、虚拟环境或本地凭据，不得删除或覆盖任何已有远程仓库。
- 遵守当前 AGENTS.md。不要自动调用或检查 Superpowers 插件中的技能，除非用户在当前消息显式使用完整 `$superpowers:<skill-name>`。

核心不变量：
- 9 张核心风控表保持表名和字段契约：`risk_rule`、`risk_event`、`risk_feature`、`risk_assessment`、`risk_case`、`risk_blacklist`、`risk_user_profile`、`risk_action_log`、`risk_alert`。
- `process_event` 的 4 步顺序和 `run_risk_check` 的 7 步顺序后续不得改变。
- 外部风险检查继续使用 `event_type/source_id/user_id/event_data` 四字段契约；现有 `order_id/receive_id` 只能作为内部兼容字段，不能成为银行 API 的新增必填参数。
- 银行事件固定为：`登录`、`转账`、`贷款申请`、`绑卡`。
- 数据库名称固定为 `bank_risk`，测试库为 `bank_risk_test`。
- 项目只使用完全虚构、掩码或哈希化的数据，不得放入真实身份证号、银行卡号、IP、手机号、密钥或客户资料。

完成以下检查点：

1. 只读盘点
- 完整阅读任务书的复用边界、银行场景和 4 个递进任务。
- 检查基线 Git 状态、项目结构、依赖、测试数量及关键耦合点。
- 特别记录业务模型、事件 Schema、validator、event、feature、ml_model、训练脚本、SQL、前端标签和测试之间的同步关系。
- 记录基线当前提交 ID 和未跟踪文件，但不改变基线状态。

2. 导入与本地基线
- 从基线当前 `HEAD` 导入已提交的项目文件到目标工作区，同时保留现有提示词文件。
- 不导入源 `.git` 和未跟踪草稿；可以读取草稿并在最终业务说明中吸收经验证的内容。
- 初始化独立 Git 仓库，主分支使用 `main`。
- 检查 `.gitignore`，确保 `.env`、密钥、数据库文件、缓存、日志、模型临时文件和个人 IDE 状态不会意外提交。
- 建立清晰的导入基线提交。提交信息说明这是电商版重构基线，不要伪装成已经完成银行化。

3. 基线验证
- 在改业务代码前运行最相关的基线测试、导入检查或静态检查。
- 将环境、命令、通过数、失败数、跳过数和已知阻塞写入 `docs/baseline-test-report.md`。
- 如果完整测试需要尚未具备的 MySQL 或依赖，先尝试安全、最小的环境准备；仍无法运行时执行可行的子集，并把准确阻塞和后续复现命令写清楚，不能写“测试通过”。

4. 业务调研与说明
- 审校基线草稿，联网检索截至当前日期仍有效的中国官方监管资料。法律和监管结论只引用全国人大/国务院/中国人民银行/国家金融监督管理总局等官方来源；每个关键结论就近给出链接和访问日期。
- 生成根目录 `1-业务说明.md`，控制在适合课堂快速阅读的 1–2 页主体，附必要来源。
- 至少包含：8 类典型风险场景、10 个关键字段、4 个事件类型及其 `source_id` 指向、5 个行业术语、数据隐私边界、教学演示而非生产决策的声明。
- 四个事件的建议映射：登录→`LoginLog.login_id`；转账→`Transaction.txn_id`；贷款申请→`LoanApplication.loan_id`；绑卡→`BankCard.card_id`。
- 明确展示信用卡/贷款/转账/登录四大业务边界，以及黑卡、代理/Tor、新设备、多卡归集、多头借贷等核心风险。

5. 重构契约和进度记录
- 创建 `docs/refactor-contract.md`，用“保持不变/必须改造/同步修改/明确不做”四部分记录边界。
- 明确 25 维特征的兼容策略：保持 14 个 `user_*`、8 个 `order_*`、3 个 `addr_*` 的形状和前缀；银行语义可重命名，但必须同步 `feature.py`、`ml_model.FEATURE_COLUMNS`、训练脚本、前端标签和测试。`order_*` 在银行版表示当前业务事件，`addr_*` 表示设备/IP/地理环境，并在文档中解释兼容命名。
- 说明规则数据进入 `sql/init_risk_data.sql`，通用 `rule.py` 只保留规则解析/匹配职责。
- 创建 `docs/goal-progress.md`，记录 Goal 1 的主要决定、验证证据和 Goal 2 前置条件，供最终 `agent_design.md` 使用。

6. 银行风控前端设计简报
- 盘点基线已有页面、模板、静态资源、前端交互和 API 绑定，区分可复用的数据交互与必须重做的电商视觉/文案。
- 创建 `docs/frontend-design-brief.md`，定义全新银行风控前端的信息架构和页面地图，至少覆盖：总览仪表盘、风险检查、规则管理、黑名单、案件管理、评估历史；仅当基线确有模型指标页面时将其纳入，不为展示效果虚构后台能力。
- 设计方向为“专业银行风险运营工作台”：以深蓝、蓝灰和中性色为基础，绿/黄/橙/红只承担通过、关注、审核、拒绝等风险语义。不得照搬任何真实银行的名称、Logo、商标或完整品牌视觉。
- 定义导航、卡片、表格、筛选器、表单、状态徽标、风险分数、规则命中详情、图表和反馈提示的视觉与交互原则，以及字体、间距、圆角、阴影、颜色等设计 token。
- 明确桌面端优先，同时验收 1440×900 和 390×844 两种视口；要求清晰的键盘焦点、足够对比度、表格窄屏策略，以及 loading/empty/error/success/高风险/黑名单等状态。
- 本阶段只形成可实施的简报和 API/数据依赖清单，不修改模板、CSS 或 JavaScript；完整实现安排在 Goal 3，最终统一打磨安排在 Goal 4。

7. 私有 GitHub 仓库
- 在任何外部写入前扫描待提交文件，确认没有 `.env`、令牌、真实 PII、数据库转储或其他秘密。
- 创建私有仓库 `ai-risk-bank`，设置为 `origin`，推送 `main`。
- 创建后回读仓库可见性，必须明确得到 `PRIVATE/private` 证据；若不能验证私有状态，不得把 Goal 标记完成。
- 将 Goal 1 文档和验证结果提交并推送。提交保持聚焦，不混入数据层代码。

验收与停止条件：
- 目标工作区已有独立 Git 历史，源基线状态没有变化。
- `1-业务说明.md`、`docs/refactor-contract.md`、`docs/frontend-design-brief.md`、`docs/baseline-test-report.md`、`docs/goal-progress.md` 均存在且内容符合要求。
- 业务说明满足任务书数量要求并有可靠官方来源，没有把法律调研表述成法律意见。
- 私有 GitHub 仓库已创建、`origin` 正确、`main` 已推送且私有状态经回读确认。
- Git 工作区干净；尚未修改银行业务表、特征、规则、pipeline 或前端实现。

工作方式与最终报告：
- 在重要检查点给出简短进度更新：当前阶段、已验证结果、下一步和阻塞；不要逐条复述普通工具调用。
- 对可逆的目标内本地修改和测试自主推进。涉及已有远程资源、破坏性操作或范围扩张时停止。
- 最终报告列出：生成文件、基线测试摘要、前端设计简报摘要、Git 提交、私有仓库链接及可见性证据、Goal 2 的已满足前置条件。未满足任一停止条件时不要宣称完成。
```
