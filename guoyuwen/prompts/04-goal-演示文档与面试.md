# Goal 4：演示、文档、面试问答与最终验收

仅在 Goal 3 验收、提交并推送后使用。推荐在同一工作区新建对话并复制下面整个代码块；若留在原对话，先执行 `/goal clear`。

```text
/goal 完成银行风控重构的“任务 4：演示 + 文档”及最终交付，持续推进直到项目可由 README 从新环境复现、银行 UI 和完整流程通过实际演练、演示素材与 5–8 分钟讲解脚本就绪、agent_design.md 完整、面试问题与参考回答文档基于真实实现生成、全量验证通过，并将最终状态推送到私有 GitHub 仓库。人工录屏本身由用户完成，不得虚假宣称已录制。

开始前必须读取：
- `1-业务说明.md`
- `docs/refactor-contract.md`
- `docs/frontend-design-brief.md`
- `docs/frontend-design.md`
- `docs/baseline-test-report.md`
- `docs/goal-progress.md`
- `docs/model-evaluation.md`
- `docs/screenshots/`
- 四类事件、25 维特征、8 个控制场景的最终代码和测试
- 当前 README、Docker 文件、模板、静态资源和所有启动脚本
- `PROMPTS_README.md` 与 `prompts/` 中四份目标提示词
- 任务书：D:\Program Files\temp_data\尚硅谷大模型项目之风控系统\AI_Risk_行业风控实战任务书.md

前置检查与授权：
- 确认 Goal 3 的代码、模型评估、截图和测试证据已推送到 private `origin/main`。
- 可以在当前项目内完成文档、Goal 3 新前端的统一打磨、Docker/启动说明、演示素材、最终测试和必要的小范围修复，并提交推送；若 Goal 3 没有真正完成设计简报要求，前置检查应判定未满足，不得在 Goal 4 从头补做并掩盖阶段缺口。
- 不新增任务书之外的产品功能，不借最终整理重构无关代码，不修改核心 9 表或四步/七步流水线。
- 只使用 `bank_risk`/`bank_risk_test` 和虚构数据；不得把凭据、真实 PII、数据库转储、日志或本机隐私放入文档、截图或仓库。
- 遵守当前 AGENTS.md。不要自动调用或检查 Superpowers 插件技能，除非用户在当前消息显式使用完整 `$superpowers:<skill-name>`。

完成以下交付物：

1. 最终银行化 UI 和文案
- 全面检查页面、表单、空状态、错误提示、规则分类、特征标签、导航、图表标题和示例，不再把用户引导到下单/支付/售后/物流字段。
- 保留并统一 Goal 3 已建立的全新银行风控设计系统，不回退到基线电商布局；本阶段只做跨页面一致性、内容层级、交互反馈、可访问性和明显可用性修复。
- 对照 `docs/frontend-design-brief.md` 与 `docs/frontend-design.md` 验证页面地图、设计 token、状态语义和 API 绑定；任何差异必须有真实实现理由并更新记录。
- 使用浏览器实际渲染并操作关键页面，至少复验 1440×900 和 390×844，检查溢出、遮挡、错误绑定、不可用按钮、空白图表、表格窄屏、键盘焦点、对比度和中文显示。
- 用 `rg` 搜索残余电商术语。迁移历史或对照文档中允许保留，但运行 UI、README 主流程和银行业务代码中的误导性残留必须处理。

2. 根目录 `README.md`
- 将 README 重写为银行风控项目启动与展示文档，结论和命令都以实际项目为准，不沿用基线的夸张数字。
- 至少包含：项目定位与免责声明、业务场景、架构、4 个事件、8 张业务表、9 张核心表、25 维特征、8 个控制场景、规则+XGBoost 双轨决策、目录结构、环境依赖、`.env.example`、MySQL/Docker 启动、数据库初始化、造数、训练、启动服务、运行测试、常见问题、截图、演示账号/样例 source（只用虚构数据）。
- 所有命令必须在 Windows PowerShell 场景下可复制；如果同时提供 Linux/macOS 命令，要清楚区分。
- 不写入真实密钥。明确项目是教学演示，不用于生产授信、反洗钱报告或自动拒贷。

3. `agent_design.md`
- 说明如何用 LLM 与四个 `/goal` 协作完成重构，引用 `prompts/` 中的真实提示词，而不是事后虚构过程。
- 至少包含：为什么拆成四个目标、每个目标的输入/输出/验收、核心不可变契约、关键技术取舍、出现过的实际失败与修正、测试与 Git 检查点、人工参与边界、Vibe Coding 的收益与局限。
- 从 `docs/goal-progress.md` 和 Git 历史提取事实；没有证据的过程不要编造。

4. 演示材料
- 创建 `docs/demo-script.md`，给出 5–8 分钟可照读但自然的讲解脚本和逐分钟操作路径：
  - 业务说明约 1 分钟；
  - 完整风险检查与案件/评估演示约 2 分钟；
  - XGBoost 数据与指标约 1 分钟；
  - 重点规则约 2 分钟；
  - Vibe Coding 与 `/goal` 复盘约 1 分钟。
- 脚本注明每一步打开的页面、使用的 `event_type/source_id/user_id`、预期命中及备用样例。
- 创建 `docs/recording-checklist.md`，列出录屏前环境、窗口、数据库、样例数据、隐私检查、音频、时间控制和失败回退。
- 复核 Goal 3 的 5–8 张重设计后截图；缺失、不清晰或与最终 UI 不一致时重新截取。README 和演示脚本引用正确相对路径。

5. 面试问题与参考回答
- 在根目录生成 `面试问题与参考回答.md`，面向 Java/Python 后端、AI 应用、风控项目或应届生项目面试。
- 生成 25–35 个高质量问题，分为以下类别：
  - 业务背景与监管/隐私；
  - 系统架构和 4 步/7 步流水线；
  - 8 张业务表与 9 张核心表；
  - 事件校验、越权防护和黑名单；
  - 25 维特征与 8 个银行控制；
  - XGBoost 数据、标签、切分、AUC/F1、泄漏与局限；
  - FastAPI/SQLAlchemy/异步数据库/测试；
  - Git、Docker、Vibe Coding 和 `/goal` 复盘；
  - 方案权衡、生产化差距和故障排查。
- 每题包含：问题、30–60 秒参考回答、可能追问及回答要点、可核对的项目证据文件/函数。回答必须来自最终代码、测试、Git 记录和 `docs/model-evaluation.md`；不得编造吞吐量、线上用户、生产告警、监管认证、模型指标或团队规模。
- 对“为什么仍使用 `order_*`/`addr_*` 前缀”“为什么黑卡拒绝 rule_count=0”“为什么不能只看 AUC”“如何避免同用户泄漏”“为什么核心流水线不改”等高概率追问给出清晰回答。
- 明确区分教学实现与生产系统：实时特征平台、模型监控、审批治理、可解释性、数据权限、灾备和监管报送属于生产化待完善项。

6. Docker、配置和启动复现
- 更新 Dockerfile、docker-compose、nginx 或相关说明中的电商项目名、数据库名、健康检查和启动命令；不增加不必要的基础设施。
- 提供安全的 `.env.example`，确认真实 `.env` 被忽略。
- 在尽可能接近新环境的条件下完整执行：依赖安装检查→数据库初始化→业务造数→风险/训练数据生成→XGBoost 训练或加载→服务启动→四事件 smoke→测试。
- 如果完整流程耗时较长，可以复用已生成数据做第二次 smoke，但 README 的首次启动路径必须至少实际验证一次。

7. 最终验证报告
- 创建 `docs/final-verification.md`，记录日期、环境、Git commit、数据库表数/关键行数、四事件结果、规则与黑名单样例、模型指标、相关测试和全量测试、前端设计简报符合性、双视口浏览器检查、Docker/启动验证、已知限制。
- 检查文档中的文件链接、命令、端口、数据库名和截图均真实存在。
- 检查 Git 跟踪文件中没有 `.env`、令牌、真实 PII、数据库转储、日志、缓存和无用训练中间文件。
- 运行完整测试。任何最终整理引入的回归都必须修复，不能靠 skip 或删除测试隐藏。

GitHub 与完成条件：
- 将最终文档、必要 UI 修复、测试和验证报告按聚焦提交提交到 `main`，推送到 private `origin`，并回读仓库可见性。
- 不创建公开镜像，不修改旧基线仓库，不把私有仓库改为 public。
- 只有 README 可复现、UI 浏览器验收、演示脚本与清单、`agent_design.md`、`面试问题与参考回答.md`、最终验证报告、全量测试和私有推送全部有证据时才标记自动化部分完成。
- 人工视频未录制不是代码阻塞，但最终报告必须明确写“已达到可直接录制状态，视频由用户按脚本录制”，不得写“视频已完成”。

最终报告按以下顺序给出：
1. 项目最终能力与验收结论；
2. 新增/更新文档清单；
3. 四事件、规则、黑名单和模型的实测摘要；
4. 测试、浏览器、Docker/启动证据；
5. Git 提交、私有仓库链接及 private 回读证据；
6. 用户接下来录屏时只需执行的最短步骤；
7. 已知限制和生产化差距。

未满足任一自动化验收条件时继续做最小必要修复，不得以“文档已写完”代替端到端验证。
```
