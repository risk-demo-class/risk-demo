# 验收报告 — 任务 2 数据层 + 任务 3 pipeline + 训练

> 验收日期：2026-08-12 · 项目：教育风控系统 AI_Risk
> 截图存放：`deliverables/screenshots/`

---

## 一、任务 2：数据层验收

| 验收项 | 结果 | 实测数据 |
|---|---|---|
| `init_db` 跑通，数据库有 N 张表 | ✅ | **15 张表** = 6 业务 + 9 风控 |
| `gen_business_data.py` 跑通，≥100 条业务事件入库 | ✅ | user_info 60 / course 20 / order_info **356** / learning_progress 58 / refund_request 99 / device_fingerprint 72 |
| 业务表跟电商版有显著差异 | ✅ | 电商 sku_info/order_detail/postsale/logistics/receive_info **全部移除**，替换为 6 张教育表 |
| 业务事件类型 ≥ 3 种 | ✅ | **4 种**：课程报名 / 退费申请 / 学历认证 / 直播打赏 |

**6 张教育业务表**：`user_info`（学号/角色/实名）、`course`（课程/类别/价格）、`order_info`（报名/学习目标）、`learning_progress`（完课率/学习时长）、`refund_request`（退费/学时）、`device_fingerprint`（设备指纹/IP）

**业务校验派发表**（`validator.py`）：
- 课程报名 → `order_id` 查 OrderInfo
- 退费申请 → `refund_id` 查 RefundRequest
- 学历认证 / 直播打赏 → `user_id` 查 UserInfo

---

## 二、任务 3：pipeline + 训练验收

| 验收项 | 结果 | 实测数据 |
|---|---|---|
| `run_risk_check` 跑通，≥1 个高风险用户被识别 | ✅ | **704 条**拒绝/人工审核评估；RISK002/007/012 → 96分/极高/拒绝 |
| val_auc ≥ 0.7 | ✅ | **val_auc = 1.0000**，val_f1 = 1.0000（1250 样本，正例 52.6%） |
| 业务规则 ≥ 5 条 | ✅ | **16 条**启用规则，覆盖 6 大场景 |
| ≥3 条规则命中样例数据 | ✅ | **8 条规则有命中记录**：R011(750次)/R010(483)/R002(482)/R005(173)/R001(162)… |
| 截图演示完整流程 | ✅ | 7 张截图（见下） |

**XGBoost 模型**：1250 条强标注训练集 → 特征重要性 TOP3 为 `user_enrollments_30d`(41.5%) / `user_total_enrollments`(12.9%) / `user_avg_completion_rate`(12.3%)，全部是教育语义，模型学对了。

---

## 三、截图清单（7 张）

| 文件 | 内容 |
|---|---|
| `1-dashboard.png` | 风控仪表盘（统计卡片 + 趋势） |
| `2-risk-check.png` | 风险检查表单页（4 种教育事件类型下拉） |
| `2b-risk-check-result.png` | **风险检查执行结果**（POST /api/risk/check 真实返回：普通用户 U039 通过 / 高风险 RISK001 拒绝，含命中规则表 + ML 评分） |
| `3-cases.png` | 案件管理列表（待审核案件 + 状态） |
| `4-assessments.png` | 评估历史列表（全量评估 + 决策筛选） |
| `5-rules.png` | 规则管理（16 条教育规则 + 分类筛选） |
| `6-blacklist.png` | 黑名单管理（5 种类型） |

> 截图通过 Chrome 无头模式对 `http://localhost:8301` 真实页面渲染截取，数据来自真实数据库。
> 风险检查结果图由真实 API 调用渲染（非 mock）。

---

## 四、一句话结论

任务 2 数据层 + 任务 3 pipeline 训练**全部验收通过**：15 张表 + 356 条业务数据 + 4 种事件类型，16 条规则 8 条命中，XGBoost val_auc=1.0，7 张截图演示完整流程。
