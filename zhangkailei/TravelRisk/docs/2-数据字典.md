# 数据字典

旅游业务表共 9 张：`travel_user`、`travel_order`、`passenger_info`、`order_passenger`、`visa_application`、`flight_booking`、`hotel_booking`、`payment_record`、`travel_blacklist_entry`。

核心风控表沿用 `risk_*` 九表。详细字段、类型和索引以 `app/models_business.py` 与 `sql/init_business_tables.sql` 为准。
