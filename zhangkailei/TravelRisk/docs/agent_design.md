# AI Agent 设计

Agent 通过八个结构化工具完成旅游事件检查、案件查询、用户画像、旅游业务查询、黑名单管理、仪表盘、趋势和规则效果分析。

- LLM 只负责理解意图、选择工具和解释结果，真实数据必须来自工具。
- 风险分、规则命中和案件状态不得由 LLM 编造。
- 查询工具默认只读。
- 黑名单修改在生产环境必须接入身份认证、RBAC、审批和审计。
- 会话历史当前在进程内存中，生产环境应迁移到 Redis 或数据库。
