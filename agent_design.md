# agent_design.md —— 我用 LLM 协作完成这个项目的方式

> 任务书要求的"Vibe Coding 复盘"：不是 AI 替我写代码这么简单，而是**人给业务上下文和验收标准，AI 产出初稿，人在真实环境里验证、再让 AI 迭代**。这份文档记录完整过程。

---

## 一、总体工作流

```
业务说明/任务书 ──▶ AI 产出 ──▶ 跑通验证 ──▶ 发现问题 ──▶ 迭代 ──▶ 验收达标
   (输入)          (初稿)      (真实 DB)      (bug/偏差)   (继续问 AI)
```

四个任务对应四次"提示词 → 产出 → 验收"循环：

| 任务 | 我给 AI 的输入 | AI 的产出 | 我的验收方式 |
|---|---|---|---|
| 1 业务理解 | 一页纸业务说明（欺诈场景/字段/事件/监管） | 银行风控业务说明 | 对照任务书场景 B 核对 |
| 2 数据层 | 业务说明 + 任务书 + 基线电商项目 | 17 张表 schema + ORM + DDL + 造数脚本 | `init_db` 跑通、行数 ≥100、DDL↔ORM 一致性脚本 |
| 3 特征/规则/模型 | 业务表 + 数据 + 任务书 | 25 维特征、11 条规则、事件生成脚本、训练 | `run_risk_check` 跑通、规则命中、val_auc/val_f1 |
| 4 文档 | 全部产出 + 验收数字 | README / 讲解稿 / 复盘 | 通读、数字回库核对 |

---

## 二、每个任务实际是怎么做的

### 任务 2：数据层（表 + 造数）

提示词核心（节选）：

> "这是我的业务说明……请帮我设计 N 张业务表 schema，写 SQLAlchemy 模型 + DDL，设计字段/关联/索引时要考虑后续风控特征计算的便利。同时写一个 Python 造数脚本，至少造 100 条业务数据。"

关键决策与理由：

- **17 张表**：任务书 B.1 的 8 张必须表（UserInfo/BankCard/Transaction/LoanApplication/LoginLog/DeviceFingerprint/IpGeoLocation/BlacklistExtra）+ 信贷生命周期扩展（合同/还款计划/还款流水/征信/收入核验/抵押/担保/企业档案/关联图谱）；
- **造数内置 7 类欺诈人群**：这样任务 3 的规则有真实样本可命中、模型有正负样本可学，不是随便造 100 条；
- **PII 哈希**：身份证/卡号/设备指纹只存 SHA-256，主动对齐个保法；
- **冗余 + 复合索引**：`(user_id, apply_time)` 支撑"近 30 天申请数"，`cert_no_hash` 支撑一房多贷，`(from_account_id, txn_time)` 支撑资金回流监控。

### 任务 3：特征 / 规则 / 模型

提示词核心（节选）：

> "这是我的业务表 + 数据……请帮我设计 3 大特征族（用户/订单/地址），字段映射到我的行业，写 compute_*_features 函数。同时调研典型欺诈套路，设计 8 条业务规则，跑通 XGBoost 训练，用 val_auc 和 val_f1 评估。"

关键决策：

- **保留基线函数名** `compute_user_features / compute_order_features / compute_address_features`，语义换成"借款人 / 事件实体 / 设备-IP"——因为 7 步流水线按前缀分类落库，改了名字就要动核心流程（任务书不许）；
- **事件特征按 event_type 字典派发**：贷款申请看申请+征信+收入核验，转账看交易金额，登录看是否凌晨；
- **训练标签来自真实跑过的评估**：先写 `gen_bank_risk_events.py` 把 1900 多条业务记录灌进 `process_event`，再训练，保证数据不是编的；
- **双轨融合 + 一票否决**：极高规则命中强制拒绝，ML 翻不了案。

---

## 三、真实迭代案例（Vibe Coding 的价值所在）

AI 初稿不是一次就对的。每个问题都是"跑起来"才暴露的，然后交给 AI 修：

| # | 问题 | 现象 | 怎么修的 |
|---|---|---|---|
| 1 | Decimal × float 混算 | 造数脚本 TypeError | 加 `U()` 工具函数，所有随机系数先转 Decimal |
| 2 | 姓名常量写成单元素元组 | `rng.choice` 选出整串 250 字名字，列超长 | 常量改成纯字符串，`choice` 取单字 |
| 3 | `--users` 数量不生效 | 传 20 却生成 50 个用户 | 欺诈人群按用户数裁剪 |
| 4 | 一房多贷/资金回流没样本 | 随机没命中，规则没得打 | 加确定性兜底：无重复权证时强制构造一组跨借款人重复抵押；资金回流/逾期人群保证放款成功 |
| 5 | 同用户并发写画像撞主键 | `Duplicate entry 'U0003' for key 'risk_user_profile.PRIMARY'` | 事件生成改成"按用户串行、用户间并行" |
| 6 | DATE 类型没有 hour | 放款事件全挂 `'datetime.date' object has no attribute 'hour'` | `_is_night()` 兼容 date |
| 7 | 训练取不到数据 | 评估全被 `ml_score IS NULL` 过滤掉 | 生成器末尾把占位 `ml_score` 重置为 NULL |
| 8 | 旧模型污染 | 电商版 25 维模型被加载，ML 分是垃圾值 | 备份旧模型，重新训练覆盖 |
| 9 | PowerShell 管道吞中文 | 查询 `'逾期'` 返回 0 | 换 UTF-8 输出编码重跑，确认是显示问题不是数据问题 |

> 这些迭代本身就是答辩素材：证明"AI 产出 + 人工验证 + 真实环境"的闭环是有效的。

---

## 四、边界：哪些 AI 不能碰

任务书明确规定，跟 AI 协作时也反复强调：

1. **风控核心 9 张表的列结构不改**——只允许换枚举词汇（事件类型/规则分类/黑名单类型），表结构动了等于推倒基线；
2. **`process_event` 4 步 + `run_risk_check` 7 步流程不改**——契约 `RiskCheckRequest(event_type, source_id, user_id, event_data)` 是硬约束；
3. **需要人拍板的业务取舍**：
   - 哪些规则设"极高/一票否决"（拦得太狠会误伤正常客户，教学场景选择拦截欺诈样本）；
   - 黑名单要不要放进主要欺诈人群（放进去会绕过规则和模型，所以黑名单演示数据刻意选了少数样本）；
   - 训练时低风险用户事件抽 35%（`--good-rate`），这是数据采样策略，AI 给方案、人定参数。

---

## 五、给老师的复盘话术（1 分钟版）

> 我用 LLM 的方式是：**先给足业务上下文，再让它产出，然后在真实数据库里跑通验证，把 bug 和业务偏差丢回去迭代**。这个项目里 AI 帮我完成了表设计、造数、特征、规则、训练脚本的全套初稿，但"哪些规则一票否决、欺诈样本怎么进黑名单、训练数据怎么采样"这类业务判断是我做的。最后 9 个真实 bug 的修复过程，恰恰说明 vibe coding 不是"复制粘贴就完事"，而是人和 AI 在真实环境里共同调试。

---

## 附：实际使用过的提示词模板

```text
任务 2（数据层）：
这是我的业务说明: <一页纸业务说明>
请帮我设计 N 张业务表 schema, 写 SQLAlchemy 模型 + DDL,
设计字段 / 关联 / 索引时要考虑后续风控特征计算的便利.
同时写一个 Python 造数脚本, 至少造 100 条业务数据.

任务 3（特征/规则/模型）：
这是我的业务表 + 数据: <schema + 样例数据>
请帮我设计 3 大特征族 (用户/订单/地址), 字段映射到我的行业,
写 compute_*_features 函数. 同时帮我调研这个行业的典型欺诈套路,
设计 8 条业务规则, 跑通 XGBoost 训练. 训练后用 val_auc 和 val_f1 评估.
```
