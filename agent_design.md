# Vibe Coding 协作说明

本项目以老师提供的电商风控系统为基线，使用 LLM 逐步完成教育行业适配：

1. 先梳理基线项目的“业务表保存事实、风险表保存判断”的分层结构。
2. 保留 7 步风控流水线、规则引擎、案件管理和 XGBoost 外壳。
3. 将业务模型替换为教育领域的用户、课程、报名、学习进度、退费与直播打赏表。
4. 通过 AI 生成并人工检查教育业务规则、样例数据和特征映射。
5. 用测试和真实数据库风险检查验证“退费申请 → 特征 → 规则 → 决策 → 案件”的完整链路。

协作原则：每次只让 AI 修改一个明确模块；修改后运行测试或真实检查，避免一次生成大量无法解释的代码。
